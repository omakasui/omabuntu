#!/bin/bash

# omakub:summary=Reapply the current Omakub theme.

THEME_NAME_PATH="$HOME/.config/omakub/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  omakub-theme-set "$(cat $THEME_NAME_PATH)"
fi
