#!/bin/bash

# omakub:summary=Reapply the current Omabuntu theme.

THEME_NAME_PATH="$HOME/.config/omakub/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  OMAKUB_THEME_SKIP_BACKGROUND=1 omakub-theme-set "$(cat $THEME_NAME_PATH)"
fi