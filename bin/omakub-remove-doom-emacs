#!/bin/bash

# omakub:summary=Remove Doom Emacs configuration for the current user.
# omakub:requires-sudo=true

echo "Removing Doom Emacs..."

omakub-pkg-drop emacs emacs-gtk
sudo rm -rf ~/.config/emacs
sudo rm -rf ~/.emacs.d
sudo rm -rf ~/.config/doom
