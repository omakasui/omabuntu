#!/bin/bash

# omakub:summary=Remove 1Password and its APT repository.
# omakub:requires-sudo=true

echo "Removing 1Password..."

sudo rm /etc/apt/sources.list.d/1password.list
sudo rm /usr/share/keyrings/1password-archive-keyring.gpg
omakub-pkg-drop 1password 1password-cli
