#!/bin/bash

# Remove one of the supported fonts. Usually called via Remove > Styles > Fonts > * in the Omadeb Menu.

if [[ -z "$1" ]]; then
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
fi


case "$1" in
jetbrains-mono)
  omakub-pkg-remove font-jetbrains-mono
  ;;
iafonts)
  omakub-pkg-remove font-ia-writer-mono
  ;;
cascadia-mono)
  omakub-pkg-remove font-cascadia-mono-nf
  ;;
mscore)
  omakub-pkg-remove ttf-mscorefonts-installer
  ;;
*)
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
  ;;
esac

fc-cache