#!/bin/bash

# omakub:summary=Remove one of the supported fonts.
# omakub:args=<jetbrains-mono|iafonts|cascadia-mono|mscore>

if [[ -z "$1" ]]; then
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
fi


case "$1" in
jetbrains-mono)
  omakub-pkg-remove fonts-jetbrains-mono
  ;;
iafonts)
  omakub-pkg-remove fonts-ia-writer-mono
  ;;
cascadia-mono)
  omakub-pkg-remove fonts-cascadia-mono-nf
  ;;
mscore)
  omakub-pkg-remove ttf-mscorefonts-installer
  ;;
*)
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
  ;;
esac

fc-cache